# see https://github.com/rochus-keller/BUSY/

subdir bode
subdir fractals
subdir casino # to just create the build directory used by die
subdir die

let apps * : Group {
	.deps = [ 
		bode.bode 
		fractals.fractals
		die.die
	]
}
