# see https://github.com/rochus-keller/BUSY/

^run_nrc( 'res/res_die', 'res_die.c', res ) 

let die * : Executable {
	.sources = [ 
		./main.c
		./dgui.c
		../casino/ddraw.c
	]
	.include_dirs += [ build_dir() ../casino ]
	.deps = [ ^all_libs res ]
	.configs += ^use_nappgui
}
